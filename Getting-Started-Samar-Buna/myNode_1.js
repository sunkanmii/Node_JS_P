function evenDoubler(a, b){
    return a + b;
}
let myVar = 9;

module.exports.evenDoubler = evenDoubler;

//npm add user to create an account on the npm registry
//Then run nom publish .(try to run this on an empty directory to verify it works(try it on a sample
//node application using npm install node_module))